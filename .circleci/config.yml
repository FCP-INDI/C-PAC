version: 2
jobs:
  build:
    machine: true
    steps:
      - checkout
      - run: docker build -t cpac:$CIRCLE_BRANCH .
      #- run: docker run -i --rm -v $HOME:/outputs -v /tmp:/scratch cpac:$CIRCLE_BRANCH s3://fcp-indi/data/Projects/ADHD200/RawDataBIDS /outputs participant --participant_label 1019436